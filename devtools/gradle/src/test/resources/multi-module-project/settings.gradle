pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id 'io.quarkus' version '999-SNAPSHOT'
    }
}
rootProject.name='mutli-module-project'

include 'common', 'application'